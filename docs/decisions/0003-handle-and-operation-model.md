# ADR-0003：代际句柄与统一 operation 模型

状态：Accepted（2026-08-11）

## 决策

32 位句柄编码类型、12 位代际和 16 位槽位。代际耗尽时槽位退役。查表只短暂持有表锁并返回共享对象，I/O 不持有全局句柄锁。

所有可阻塞调用建立统一 operation，以原子一次完成点竞争成功、超时、取消和关闭；使用单调绝对 deadline。

## 后果

过期句柄可可靠拒绝；阻塞读取可由 `viTerminate`/`viClose` 唤醒；未来异步 API 可复用同一模型，而无需重写同步语义。
