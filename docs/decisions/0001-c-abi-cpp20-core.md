# ADR-0001：稳定 C ABI 与 C++20 内核

状态：Accepted（2026-08-11）

## 背景

VISA 生态以 C 接口互操作，但内部需要安全的生命周期、并发和可扩展后端。

## 决策

公共边界使用固定布局 C ABI；内部使用 C++20。异常不得越过 ABI，STL 与 C++ 对象不得出现在公共签名中。共享库默认隐藏符号，只导出标记 API。

## 后果

C 和 C++ 头编译、符号检查与结构尺寸成为门禁。`0.x` 仅承诺可迁移的源码兼容，稳定版前再冻结二进制 ABI。
