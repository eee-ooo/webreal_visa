# ADR-0004：尺寸协商的版本化插件契约

状态：Accepted（2026-08-11）

## 决策

插件 ABI 使用 C 结构，包含 ABI 主/次版本、`struct_size`、能力位和宿主服务回调。唯一入口为版本化 `wrvisa_plugin_query` 契约。插件受控加载一次，进程运行期不卸载。

## 后果

结构尾部可向后兼容扩展，主版本处理破坏性变化。`0.1` 只声明 ABI，不实现加载器，避免在真实需求出现前固化生命周期细节。
