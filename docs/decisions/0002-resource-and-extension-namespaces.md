# ADR-0002：资源语法与扩展命名空间分层

状态：Accepted（2026-08-11）

## 决策

标准资源按 VPP-4.3 解析和规范化。非标准能力必须标为 `DE_FACTO_EXTENSION` 或 `PROJECT_EXTENSION`。模拟资源固定为 `WRVISA0::MOCK::INSTR`，只用于测试；不得伪装成 TCPIP/GPIB 等真实资源。

查找表达式使用项目自己的 VPP 语法解析器，不把宿主正则表达式方言直接暴露为兼容行为。

## 后果

解析成功与可打开是两个不同能力。未来后端注册按资源接口类型匹配，不改变解析 API。
