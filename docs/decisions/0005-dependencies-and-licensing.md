# ADR-0005：最小依赖与许可证隔离

状态：Accepted（2026-08-11）

## 决策

`0.1` 只依赖语言标准库。后续 TCP/串口优先评估 standalone Asio，USB 通过可替换 libusb 动态边界，VXI-11 使用最小自有 ONC RPC/XDR；每次引入需单独 ADR、固定版本和许可证台账。

GPL 等强 copyleft 实现不链接、不复制、不形成派生代码；LGPL 组件必须可替换并纳入发行合规。

## 后果

当前库易于复核且没有第三方分发义务。协议后端的开发量上升，但不会提前锁死不合适的许可证或 API。
